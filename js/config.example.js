// Renomeie este arquivo para config.js e preencha com os valores reais
// NUNCA versione o config.js — ele está no .gitignore

export const FIREBASE_CONFIG = {
  apiKey:            'SUBSTITUA',
  authDomain:        'SUBSTITUA',
  projectId:         'SUBSTITUA',
  storageBucket:     'SUBSTITUA',
  messagingSenderId: 'SUBSTITUA',
  appId:             'SUBSTITUA',
}

export const DEMO_CREDENTIALS = {
  email:    'demo@praxis.app',
  password: 'SUBSTITUA',
}
